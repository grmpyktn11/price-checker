[pytest]
# tests marked live hit the real retailers and the real Claude API: they cost money, get
# rate-limited and take minutes, so the default run excludes them. run them deliberately
# with: pytest -m live
addopts = -m "not live"
markers =
    live: makes real network calls (retailers, reddit/youtube, Anthropic)
