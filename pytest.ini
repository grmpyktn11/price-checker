[pytest]
# tests marked live call the real Claude API: they cost money and take minutes, so the
# default run excludes them. run them deliberately with: pytest -m live
addopts = -m "not live"
markers =
    live: makes a real Anthropic API call
